# Memory kit update receipt — 2026-09-05

Evidence state: package/fixture verified; native runtime unverified.
Goal: update agent-memory-kits against current RecursiveIntell work while keeping
native memory, replication, compaction and execution owners authoritative.

Repository: RecursiveIntell/agent-memory-kits
Branch: update/current-memory-stack-20260905
Commit before: 44c758dc77404e92099173d47ce81666a7fe08b8 (clean checkout)
Commit after: the commit containing this receipt; no self-referential SHA claim.
Dirty state: all listed task edits originate in this clean task checkout.

## What changed

- Canonical launcher plus generated standalone Codex/Claude payloads; Hermes
  delegates to it. Explicit embedding, authority-token-file, codec and Mnemes
  journal settings either reach the native owner or fail.
- Doctor and Python recall subprocesses use that configuration path, with listeners
  disabled for probes. The implicit retry without the requested embedder is removed.
- Fresh host templates default to stdio. Claude's hardcoded relay is removed.
  Legacy Hermes token inputs are explicitly rejected in favor of token files.
- Admin launcher no longer fabricates a reembedding intent/receipt on startup.
- Native governor audit has nonzero typed failure for unavailable, failed or malformed
  owner responses; no synthetic passing compression audit or no-op ledger-write flag.
- Correct initialized MCP discovery, response-ID binding, temporary mock stores,
  current profile names and native governor capability observation.
- Shared capture/governed-memory skills with generated host copies; updated host
  guidance, plugin metadata and revision-pinned current-stack owner map.
- CI runs static package validation, generated-copy checking and Python behaviors.
  Native validation remains the default of validate-all-kits.sh and a separate gate.

Files touched: shared launch/config/audit/discovery/rules/skills; Codex, Claude and
Hermes adapters/manifests; six MCP-only host templates/README links; tests; CI;
scripts/sync-kit-assets.py; docs/CURRENT_STACK.md and upstream-sources.json.
The Git commit diff is the exact file inventory.

## Commands run with exit status:

- `python3 -m pytest -q` (baseline): exit=1, 207 passed, 14 failed, 3 skipped.
- `python3 -m pytest -q -rs` (final source): exit=0, 219 passed, 19 skipped, 17 subtests passed.
- `./scripts/validate-all-kits.sh` (native mode): exit=1, semantic-memory-mcp and context-governor binaries missing.
- `./scripts/validate-all-kits.sh --static`: exit=0, package syntax, JSON, generated copies and companion tools/list pass.
- `python3 scripts/sync-kit-assets.py --check`: exit=0.
- `python3 /root/.codex/skills/.system/skill-creator/scripts/quick_validate.py shared/skills/governed-memory`: exit=0.
- `python3 /root/.codex/skills/.system/skill-creator/scripts/quick_validate.py shared/skills/memory-capture`: exit=0.
- `python3 /root/.codex/skills/.system/skill-creator/scripts/quick_validate.py codex/plugins/semantic-memory/skills/semantic-memory`: exit=0.
- `git diff --check`: exit=0.

Passing checks: configuration forwarding/rejection across hosts; doctor configuration
fidelity; native-owner failure behavior; initialized MCP discovery; isolated store
cleanup; generated payload parity; existing recall and authority regressions.
Failing checks: native validation cannot locate installed Rust binaries.
Skipped checks with reasons: six context-governor native tests (missing executable),
ten official STALE cases (absent dataset), three existing optional Sleeper cases
(absent checkout). No mock output substitutes for these gates. Cargo is absent;
Rust builds, clean registry installation and upstream conformance suites did not run.

## Independent audit

First audit found AMK-01 (Hermes configuration bypass) and AMK-02 (doctor bypass).
Its failed receipt is retained in 2026-09-05-independent-audit.json. A bounded
follow-up repaired both under the existing authorization. Independent reinspection
reported both closed, 37 targeted tests and four subtests passing, isolated
standalone launcher smoke passing, and no remaining material defect in its scope.
The final HTTP-default delta was independently checked: 8 tests and four subtests
passed across templates, generated configs and the corrected Codex skill.

Source-of-truth decisions: Rust/tool owners remain canonical; shared kit sources
own only wiring/instructions; plugin copies are generated projections.
Migrations or compatibility effects: no live data, devices, membership, keys,
services or installed host configuration changed. Unsupported options now fail;
removed relay/token/no-op options require explicit configuration changes.
Known risks: native build/host integration remains unproven. Legacy HTTP hooks and
V1 compaction helpers are not certified as authenticated HTTP or governed V2
integration. Codex hook/server historical store defaults differ; the guide requires
an explicit shared directory, without moving or merging existing stores.
Proof debt: see native/external skips and CURRENT_STACK.md activation gates.
Release blockers: no production or native release certification; merge/release
requires the relevant installed-host gates and current CI result.
Rollback: revert this kit commit. No data migration needs reversal. A later native
binary upgrade requires its owner's backup/restore procedure.
Remaining delta: validate selected native binaries, host hooks, V2 recovery and
physical-device canary separately before making those operational claims.
Auditor rerun commands: install tests/requirements.txt; run pytest -q -rs,
validate-all-kits.sh --static, sync-kit-assets.py --check; on a configured installed
host run validate-all-kits.sh --native.
