# Current memory stack refresh

Source baseline: agent-memory-kits 44c758dc77404e92099173d47ce81666a7fe08b8, clean main.
Authority: user requested GitHub kit updates; deliver on a reviewable branch/PR.

## Findings and bounded phases

1. HIGH / confirmed_static / launch configuration: three launchers own divergent
   option forwarding; two drop authority-token files; unsupported flags disappear.
   Claude's default relay uses a machine-specific absolute path. Shared launch also
   enables HTTP without configuring the authentication now required upstream.
   Fix: one generated launcher implementation, preserve host store/profile defaults,
   pass explicitly requested options or reject; stdio default, relay opt-in removed.
   Gate: disposable fake-binary argv/failure tests across shared/Codex/Claude.
2. MEDIUM / claim_mismatch / skill and discovery surfaces: capture instructions call
   tools absent from default profiles; generator probes a nonexistent admin profile
   without initialize and uses a persistent /tmp store. Fix: witnessed-first capture,
   current-profile discovery, isolated correctly initialized tools/list probes.
   Gate: MCP protocol fixture and generated-copy checks.
3. HIGH / confirmed_static / audit wrapper: missing/failed Rust owner exits success;
   Python heuristic can emit a passing compression audit under the owner schema.
   Fix: typed unavailable/failed output and nonzero exit; preserve native receipts.
   Gate: missing owner, malformed response, failing owner, and valid native response.
4. MEDIUM / confirmed_executed / validation: baseline 207 passed, 14 failed, 3 skipped.
   Four failures require absent context-governor; ten require absent official STALE
   data. CI currently only syntax-checks Python. Fix: run behavioral suite; mark
   external-fixture/native dependencies explicitly with reasons, keep local negative
   contracts executable. Gate: clean-checkout pytest and kit validation.
5. MEDIUM / claim_mismatch / source guidance: Libraries default branch is July 16;
   main is September 4. Add immutable source/owner coverage and upgrade gates for
   September memory integrity, context recovery, Mnemes, graph execution, codecs,
   and Ares specialist work. Gate: referenced source paths/revisions checked locally.

## Ownership and invariants

semantic-memory owns memory truth, temporal state, provenance and repair;
semantic-memory-mcp owns CLI/profile/tool schemas; context-governor owns compaction
receipts; Mnemes owns replication; agent-graph/recursive-agent own execution.
Kits own host wiring and instructions only. Generated copies are projections.
Do not copy Rust contracts into Python, migrate stores, enable remote services,
provision tokens/devices, broaden default authority, or claim host/runtime proof.

## Rollback and stop conditions

All changes are confined to this kit branch. Revert the branch commit to restore
files; no store migration or live configuration writes occur. Existing store paths
are preserved, including Claude's directory whose name ends in .db. Reject a file
at a configured directory path rather than inventing a new store. Stop on unknown
ownership or changes requiring live devices, private tokens, or core semantics.
Independent terminal audit follows implementation; any findings need a bounded
follow-up repair, never edits during that audit.

## Bounded follow-up after independent audit

AMK-01 and AMK-02 confirmed Hermes launcher and doctor still bypass shared settings.
Under the existing kit-update authority, integrate those two entrypoints, retain
Hermes token-file transport rules (explicitly reject obsolete token-value settings),
add Hermes/doctor configuration fixtures, and request independent re-verification.
No upstream semantic edits, native store changes or extra permissions are needed.
